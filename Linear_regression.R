## Linear Regression ###########################################################

# If x increases, how does y change?

# Find the best straight line to predict a number


# The model = y=β0​+β1​x

# y = what you’re predicting
# x = your input (feature)
# β₀ = intercept (where the line starts)
# β₁ = slope (how much y changes when x increases by 1)

# error=(yactual​−ypredicted​)2 - (least squares for minimising error)

# Pick β₀ and β₁ so the line hugs the data as tightly as possible

